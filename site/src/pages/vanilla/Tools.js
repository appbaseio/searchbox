import React from 'react';
import config from '../../constants/config/tools/vanilla';
import Tools from '../../components/Tools';
import theme from '../../constants/theme/vanilla';

export default () => <Tools config={config} theme={theme} />;
