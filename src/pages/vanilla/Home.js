import React from 'react';
import config from '../../constants/config/home/vanilla';
import Home from '../../components/Home';
import theme from '../../constants/theme/vanilla';

export default () => <Home config={config} theme={theme} />;
